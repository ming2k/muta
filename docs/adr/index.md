# Architecture Decision Records

Durable records of significant technical decisions and their context. Each ADR
is a short Markdown file numbered `NNNN-<slug>.md`. Once a decision is final its
status is `Accepted`; a later ADR supersedes an earlier one rather than editing
it in place.

See [ADR Workflow](../dev/documentation/adr-workflow.md) for the process.

## Index

| ADR | Title | Status |
|-----|-------|--------|
| [0001](0001-tool-rendering-redesign.md) | Tool-step rendering redesign: log entries over expandable cards | Accepted |
| [0002](0002-model-channel-abstraction.md) | Model/channel abstraction and picker redesign | Accepted |
| [0003](0003-extract-neenee-app-crate.md) | Extract `neenee-app` from the binary crate | Superseded by ADR-0004 |
| [0004](0004-six-crate-topology.md) | Six-crate topology: core / app / providers / tools / harness / cli | Superseded by ADR-0005 |
| [0005](0005-strict-layering-and-renames.md) | Strictly-layered topology + scenario-bound store | Accepted |
| [0006](0006-plan-mode-v2.md) | Plan mode v2: approval gate, active plan path, proposed-plan rendering | Superseded by ADR-0027 |
| [0007](0007-plan-progress-panel.md) | Plan progress sticky panel above input box | Superseded by ADR-0020 |
| [0008](0008-single-breathing-anchor.md) | Single breathing anchor for TUI liveness | Accepted |
| [0009](0009-uncapped-agentic-loop.md) | Uncapped agentic loop (remove per-turn round cap and `/loop` iteration cap) | Accepted |
| 0010 | Slim the goal primitive (drop status machine, token budget, time accounting) | Superseded by ADR-0082 |
| [0011](0011-subagent-profiles.md) | Sub-agent profiles: capability-axis tool admission (`requires_user` / `spawns_subagent` + `EXPLORE` profile) | Accepted |
| [0012](0012-toolaccess-tier-split.md) | `ToolAccess` tier split (`Read < Execute < Write`) and the `VERIFY` profile | Accepted |
| [0013](0013-skills-xdg-paths-and-bundled-embed.md) | Skills & commands: XDG paths + compile-time-embedded bundled skills | Superseded by ADR-0058 |
| [0014](0014-xdg-persistence-architecture.md) | Unified XDG persistence architecture (single `Dirs` policy, four-category model, fixed override precedence) | Partially superseded by ADR-0130 |
| 0015 | Pursue stop-gate + repeat cron scheduler (replace `/goal` + `/loop`) | Superseded by ADR-0082 |
| [0016](0016-session-review-over-round-counting.md) | Session review over round-counting stall detection (diagnostic sub-agent + opt-in hard stop) | Accepted |
| [0017](0017-side-conversations.md) | Side conversations: session-native `/btw` (concurrent parent + side, event envelope, `fork_to_side`) | Partially superseded by [0103](0103-btw-background-asides.md) |
| [0018](0018-per-project-multi-instance-concurrency.md) | Per-project multi-instance concurrency (drop the per-project flock; one session file per live instance; file-scoped locks on shared global state) | Accepted |
| [0019](0019-model-relative-context-compaction.md) | Model-relative context compaction (token thresholds derived from the active model's context window) | Accepted |
| [0020](0020-unified-task-list.md) | Unified task list (supersede the per-plan progress panel with one shared `TodoList`) | Accepted |
| [0021](0021-pruning-is-implicit-and-distinct-from-compaction.md) | Tool-result pruning is implicit (silent, gated at ~65%) and renamed distinct from compaction (`ContextRelief*`) | Accepted |
| [0022](0022-session-level-ai-title.md) | Session-level AI title (first-turn auto + on-demand `/title` + manual lock; `TITLE` sub-agent profile) | Accepted |
| [0023](0023-relevance-aware-tiered-pruning-and-layered-token-accounting.md) | Relevance-aware, tiered pruning (staleness/keep-alive, truncate→clear, informative placeholders) and layered token accounting (`effective_pressure_tokens`) | Accepted |
| [0024](0024-pragmatic-sqlite-migrations.md) | Pragmatic SQLite migrations via `PRAGMA user_version` | Superseded (SQLite removed) |
| [0025](0025-lifecycle-event-hooks.md) | Lifecycle event hooks (single event axis: SessionStart/End, UserPromptSubmit, PreToolUse/PostToolUse/Stop/PreCompact; command-handler config; delete one-shot `CompactionHooks`) | Accepted |
| [0026](0026-plan-progression-forcing-functions.md) | Plan progression forcing functions (plan-exit nudge, todo-continuation nudge, approval-handoff instruction; re-order verify-nudge after the todo list drains) | Superseded by ADR-0033 |
| [0027](0027-plan-as-subagent.md) | Plan as a subagent (replace Plan mode with a `PLAN` profile + a `plan` tool; supersedes ADR-0006, revises ADR-0026; depends on ADR-0028/0029) | Superseded by ADR-0033 |
| [0028](0028-capability-allocation-scoped-writes.md) | Capability allocation: scoped filesystem writes (`WriteScope` per agent + `write_paths` grant on `ToolPolicy`; decouples write admission from the `ToolAccess` ceiling) | Superseded by ADR-0084 |
| [0029](0029-full-duplex-subagent-communication.md) | Full-duplex subagent communication (steering `AgentOp` inbox + `SubagentHandle` request/reply + `SubagentRegistry` keyed by parent tool-call id) | Accepted |
| [0030](0030-early-loop-intervention-and-round-hook.md) | Early loop intervention (in-loop semantic review + anti-anchoring nudge via a `steering` module) and a constrained `Deny`-forbidden round-count hook (partially supersedes ADR-0025) | Accepted |
| 0031 | Remove the pursuit tools (`get_pursuit` / `start_pursuit` / `complete_pursuit`); the `/pursue` slash command, stop-gate, and `[NEENEE_PURSUIT_COMPLETE]` marker own the lifecycle (reverses the tool-keeping sub-decisions of ADR-0010/0015) | Superseded by ADR-0082 |
| 0032 | Fold pursuit persistence into `SessionStore` (delete `PursuitStore` / `PursuitService` / `pursuits.db`; move `pursuit` onto `SessionData` + `SessionEvent::PursuitSet`; drop the `pursuit_service` field from `Agent` and every turn context) | Superseded by ADR-0082 |
| [0033](0033-remove-plan-and-verify-workflow.md) | Remove the plan-as-subagent and verify workflow (`plan` / `verify_plan_execution` tools, `PLAN` / `VERIFY` profiles, plan-exit / todo-continuation / verify nudges, `MAX_REPEATED_TOOL_CALLS`; supersedes ADR-0026 / ADR-0027, narrows ADR-0012) | Accepted |
| [0034](0034-range-aware-pruning-and-deterministic-read-loop-guard.md) | Range-aware prune staleness (a read is stale only when a later read covers its line range, or the file is mutated — paging no longer self-evicts; refines ADR-0023) and a deterministic, non-terminating read-loop guard (frequency-window detection + anti-anchoring nudge; revives ADR-0030's intervention without a model call) | Accepted |
| [0035](0035-application-layer-split.md) | Application-layer split: `neenee-code` + `neenee-quant` (rename cli→code; add the quant application crate + `QUANT` profile) | Superseded by ADR-0073 / ADR-0075 |
| [0036](0036-cjk-wide-char-ghost-cells.md) | Heal CJK wide-character "ghost" cells with a whole-row re-emitting backend wrapper (`WideHealBackend`) so wide-glyph trailing columns stay fresh through tmux | Superseded by ADR-0038 |
| [0037](0037-server-layer.md) | Session/server layer (`neenee-server`): multi-session daemon + multi-frontend (`SharedState` / `SessionRegistry` / `SessionHandle`; `mpsc`→`broadcast`) | Accepted |
| [0038](0038-in-house-grid-diff-rendering-engine.md) | Replace ratatui with an in-house grid + diff rendering engine (`neenee-tui`: vim-style retained grid, write-marks-dirty diff, crossterm backend, `bce` awareness); supersedes ADR-0036 | Accepted |
| [0039](0039-unified-prompt-registry.md) | Unified prompt registry: declarative system-channel composition via `PromptSection` (one trait + one registry keyed by `InjectionKind` replace the ad-hoc `format!`/`push_str` system-prompt assembly; the two duplicated turn-loop prep funnels collapse to one; two latent sub-agent system-message clobber defects fixed). User channel and store prompts investigated and deliberately not migrated | Superseded by ADR-0056 |
| [0040](0040-session-state-and-context-projection.md) | Session state and model-context projection vocabulary (`SessionData.model_window`, `archived_transcript`, `ContextProjection*`; legacy `ContextRelief*` aliases retained on disk) | Accepted |
| [0041](0041-tool-capabilities-scope-and-override.md) | Tool capabilities and variants: two orthogonal axes — scope (agent/profile: which capabilities) vs override (model: which variant via `[tool_variants]`); `ToolSet`/`Capability` registry; subagents inherit the parent model's variant selection; replaces the `[tool_overrides]` string-patch layer | Accepted |
| [0042](0042-principal-envoy-role-vocabulary.md) | Principal / Envoy role vocabulary: keep `agent` as the umbrella engine term; name the top-level role `Principal` (`[principal]` config) and the spawned child role `Envoy` (renames the `subagent` tool/types/files); hard rename, no config alias | Accepted |
| [0043](0043-bash-stdin-execution-contract.md) | Bash stdin execution contract: non-interactive by construction (`StdinPolicy` hard floor + idle watchdog + interactive classifier + `\r`-aware capture + themed termination footer + human/model input injection) | Accepted |
| [0044](0044-layered-token-accounting.md) | Layered token accounting: provider `usage` is authoritative when present (`usage_supported`/`take_last_usage` + `ProviderStreamEvent::Usage`), falling back to a char-class estimator that is accurate for CJK + code (replaces `bytes/4`); a `TokenSourceLedger` attributes every token as reported vs estimated and a report modal surfaces the accuracy | Accepted |
| [0045](0045-extract-neenee-tui-view.md) | Extract `neenee-tui-view` (widgets + semantic document model) from the `neenee-code` app shell; three-layer engine/view/shell topology with a one-way `TranscriptView<'a>` seam enforced by the compiler | Superseded by ADR-0079 |
| [0046](0046-reasoning-is-opt-in-per-model.md) | Extended thinking is opt-in and controlled per model (default off; the `[model_reasoning]` entry is the single opt-in surface; effort/thinking removed from the provider level) | Accepted |
| [0047](0047-round-contains-turn-vocabulary.md) | Round contains turn vocabulary swap: relabel the two execution layers so **round** is the user-perceived unit and **turn** is the in-round ReAct iteration (inverse of the prior convention; pure rename — `execute_turn`→`execute_round`, `TurnEvent`→`RoundEvent`, `hard_stop_rounds`→`hard_stop_turns`, `Round` hook event→`Turn`; breaking config rename) | Accepted |
| [0048](0048-session-as-single-source-of-truth.md) | Session as the single source of truth: converge message truth onto `SessionStore` (eliminate the parallel `history` working copy; add `mutate_messages` atomic primitive), move session-scoped runtime state (`disabled_tools`/`turn_counter`/pursuit armed+iterations) into `SessionData`, and make network serialization a pure projection of the session | Accepted |
| [0049](0049-meta-strip-render-component.md) | `MetaStrip` render component for transcript metadata headers: extract one reusable one-row strip (`MetaStrip`/`MetaChip`/`MetaTone`) that both the assistant round header and the sent user-message header compose, and add a `▌` turn gutter rail so the larger user-visible scope gets a stronger anchor than the in-round band | Accepted |
| [0050](0050-non-driving-command-echoes.md) | Non-driving command echoes in the durable transcript: persist slash/shell invocations as provenance-stamped (`InjectionKind::CommandEcho`) user messages that survive resume/export but are projected out before the provider wire (new "durable-but-not-model-visible" category; reuses `origin`, does not touch `hidden`) | Accepted |
| [0051](0051-disposable-tool-results.md) | Disposable tool results: per-tool declaration for early pruning | Accepted |
| [0052](0052-xai-supergrok-provider.md) | xAI (Grok / SuperGrok) provider | Accepted |
| [0053](0053-declarative-principal-profile.md) | Declarative Principal profiles: a `PrincipalProfile` value mirroring `EnvoyProfile`, `Agent::apply_principal_profile`, and `principal_code()` in the server layer; `AgentIdentity` relocated to core as role vocabulary | Accepted |
| [0054](0054-server-layer-followups.md) | Server-layer follow-ups: relocate identity (`principal_code`/`neenee_identity`) from server to the application layer; secure serve defaults (loopback by default, `--public` forces bearer token); `SlashCommandHandler` extension point for application-registered Rust slash commands | Accepted |
| [0055](0055-session-scoped-request-lifecycle-accounting.md) | Session-scoped request lifecycle accounting: separate current context from request usage; persist idempotent session/actor/round/turn/attempt records across completion, interruption, failure, retry, crash recovery, side sessions, and envoys | Accepted |
| [0056](0056-model-context-assembly-boundary.md) | Model-context assembly boundary: specialize the system-prompt registry, centralize harness-authored context-message construction, and make request preparation the single provider-facing funnel | Accepted |
| [0057](0057-contract-only-core-boundary.md) | Contract-only core boundary: retain the cycle-breaking domain contract crate while relocating agent-owned pure policy and runtime state | Accepted |
| [0058](0058-remove-bundled-skill-tier.md) | Remove the bundled skill tier while retaining XDG and project skill sources | Accepted |
| [0059](0059-agent-tool-integration-boundary.md) | Agent-tool integration boundary: agent consumes concrete tools through a normal downward dependency | Accepted |
| [0060](0060-skills-and-mcp-extension-boundaries.md) | Separate skill capability and MCP connector boundaries with a generic dynamic-tool publication port | Accepted |
| [0061](0061-atomic-model-request-boundary.md) | Atomic model request boundary: pair messages and tool declarations while separating durable conversation context from ephemeral request assembly | Accepted |
| [0062](0062-longport-openapi-quant-adapter.md) | Direct LongPort OpenAPI adapter for quantitative trading | Superseded by ADR-0073 |
| [0063](0063-intelligence-workbench-and-expert-council.md) | Intelligence workbench and expert council boundary | Superseded by ADR-0073 |
| [0064](0064-product-family-workspace-layout.md) | Product-family workspace layout | Superseded by ADR-0073 |
| [0065](0065-runtime-fitted-model-capability-overlay.md) | Runtime-fitted model capability overlay: trusted provider templates fit metadata for registry-unknown ids from live `/models` (persisted per instance, overlaid onto `model::resolve` behind the static registry); `kimi-code` opts in first | Accepted |
| [0066](0066-dual-write-provider-selection.md) | Dual-write provider/model selection: a `/provider` switch or add-provider flow persists the selection as the global `config.toml` default **and** pins it to the session, so a fresh launch follows the switch while resume restores the session's own model | Accepted |
| [0067](0067-modular-prompt-cache-control.md) | Modular prompt-cache control policy: a pure-domain `CachePolicy` classifier (`Breakpoints`/`SessionKey`/`Automatic`) per model family, a shared `read_cached_tokens` helper so OpenAI/Gemini/Moonshot discounts surface in the token-source report (not just Anthropic), and session-id-keyed `prompt_cache_key` injection for Moonshot/Kimi | Accepted |
| [0068](0068-system-reminder-dynamic-injection.md) | System-reminder dynamic injection layer: a two-tier XML trust model — `<system-reminder>` authoritative directives (`InjectionKind::SystemReminder`) vs `<untrusted_…>` escaped data (`InjectionKind::UntrustedDirective`) — plus a `ReminderSink` accumulator, giving mid-turn event-driven instructions a canonical channel distinct from the rebuilt head system prompt | Accepted |
| 0069 | Pursuit budgets and runtime stats: optional opt-in `PursuitBudget` (turns/tokens/wall-clock) set via `/pursue budget`, session-scoped `PursuitStats` accumulated each round and surfaced in `/pursue status`, budget hard-stop with `terminal_reason`, and a ≥75% convergence reminder — all while keeping the marker-based stop-gate (no LLM judge) | Superseded by ADR-0082 |
| [0070](0070-provider-scoped-remote-model-metadata.md) | Provider-scoped remote model metadata: trusted provider model catalogues persist endpoint and capability facts per channel; remote fields override the static baseline only for that provider route | Accepted |
| [0071](0071-defer-kernel-split-and-backport-strictness.md) | Defer the kernel split; back-port strictness from the forks: keep the product-family monolith (no kernel/protocol crate extraction without a second consumer) while landing the forks' split-independent fixes — `NoProvider` sentinel, tool-argument schema validation, stream-truncation detection, streaming Anthropic usage merge | Accepted |
| [0072](0072-type-level-secret-redaction.md) | Type-level secret redaction: a `SecretString` newtype (redacted `Debug`/`Display`, transparent serde, explicit `expose_secret()`) backs every credential field in config, auth, web-search, and wire types; transport errors mask credential URL query parameters | Accepted |
| [0073](0073-flat-coding-focused-workspace.md) | Flat coding-focused workspace: collapse to one `crates/` directory and remove the editor/quant products | Accepted |
| [0074](0074-consolidate-llm-client-crate.md) | Consolidate the four `neenee-ai-sdk-*` crates into one pooled `neenee-llm-client` (transport + protocol modules) | Accepted |
| [0075](0075-rename-neenee-code-to-neenee.md) | Rename `neenee-code` → `neenee` (single-product rename: reverse the ADR-0035 cli→code rename now that editor and quant are gone; package renamed again by ADR-0080) | Accepted |
| [0076](0076-rename-session-and-store-crates.md) | Rename `neenee-session` → `neenee-transport` and `neenee-store` → `neenee-persistence` (vocabulary cleanup: free "session" to mean only the persisted conversation; name the transport and persistence layers for what they do) | Accepted |
| [0077](0077-rename-neenee-auth-to-neenee-oauth.md) | Rename `neenee-auth` → `neenee-oauth` (vocabulary cleanup: the crate does only OAuth2 credential acquisition — API-key auth lives in `neenee-persistence` — so the name matches the `OAuth` facade and frees "auth" to mean the `ChannelAuth` concept in `neenee-core`) | Accepted |
| [0078](0078-round-lifecycle-type.md) | Round lifecycle in one type (`RoundLifecycle` owns the supersede/cancel/finish protocol previously copied across nine sites) and a typed `LoopStatus` badge replacing the stringly `loop_status`; explicitly not a runner crate or state machine (respects ADR-0071) | Accepted |
| [0079](0079-remerge-tui-view-into-binary.md) | Re-merge `neenee-tui-view` into the application binary (single consumer + 92% lockstep change; view modules land flat in `crates/neenee-cli/src/tui/`; the engine/view/shell seam becomes convention again) | Accepted |
| [0080](0080-rename-neenee-to-neenee-cli.md) | Rename `neenee` → `neenee-cli` (package); the command stays `neenee` | Superseded by ADR-0136 |
| [0081](0081-neenee-server-and-attach-model.md) | Standalone `neenee-server` binary and the attach model (`bootstrap::assemble` factory; headless one-session host + discovery file; `neenee --attach` TUI-as-client) | Partially superseded by ADR-0089 / ADR-0096 |
| [0082](0082-remove-pursuit-stop-gate.md) | Remove the pursuit stop-gate and primitive entirely (`/pursue`, marker, gate, budgets, checkpoints); a round ends when the model stops calling tools; `/repeat` kept | Accepted |
| 0083 | Crash-consistent pursuit attempt accounting: persist pass/token/time counters with the armed runtime, align checkpoints and the 50-pass cap to one unit, type checkpoint status, and record terminal reasons on every non-completion path (supersedes ADR-0069's in-memory-statistics decision) | Superseded by ADR-0082 |
| [0084](0084-soft-write-scope-gate.md) | Soft write/operation-scope gate: an out-of-scope call is routed to the permission broker for the user to decide when attended, and hard-denied only when autopilot (supersedes ADR-0028's hard-block decision) | Accepted |
| [0085](0085-config-time-tool-scoping.md) | Config-time tool scoping: unified external-tool sources (global + project `.neenee/config.toml`, merge with trust model), eager full-schema loading, `/reload` hot reload, no runtime progressive disclosure (retire the idle disclosure machinery) | Accepted |
| [0086](0086-coding-envoy-profile.md) | The `CODE` envoy profile and the `envoy_code` dispatch tool: a write-capable, user-supervised coding sub-agent (admits `bash`/`edit_file`/`write_file` by name; `autopilot: false` routes every side effect through the broker); the second `EnvoyTool` instance shares the registry under a distinct tool name | Superseded by ADR-0087 (supervision decision only) |
| [0087](0087-code-envoy-runs-autopilot.md) | The `CODE` envoy runs on autopilot: delegation via `envoy_code` *is* the authorization, so the child's writes/commands no longer route through the principal's permission broker; `ask_user` supervision is preserved. Also routes `envoy_code` through the envoy TUI step renderer | Accepted |
| [0088](0088-command-acknowledgment-toast-notices.md) | Command acknowledgments as ephemeral toast notices: route slash-command *reply* confirmations (e.g. `/autopilot on`) through the dormant `NoticeSurface::Toast` path as a transient top-right bubble instead of `RoundEvent::Text`, so they are visually distinct from model output and no longer pollute the transcript (ADR-0050's durable *invocation* is untouched) | Accepted |
| [0089](0089-multi-session-daemon.md) | Multi-session daemon: a `SessionRegistry` hosts N sessions in one process; the select-then-attach wire protocol (`Select`/`Welcome`/`Pick`) replaces immediate `History`; `neenee daemon` subcommand; `--attach` becomes `attach` (revises ADR-0081) | Partially superseded by ADR-0094 / ADR-0096 |
| [0090](0090-scheduled-prompt-unification.md) | Scheduled-prompt unification: generalize `RepeatJob` into `ScheduledJob { trigger: Schedule::Cron \| Schedule::Once }`; add `/schedule` accepting cron (recurring) or countdown/absolute-time (one-shot); `/repeat` becomes a cron-only alias; session-schema v9 with full back-compat (serde aliases + legacy-flat deserializer) | Accepted |
| [0091](0091-command-ledger-and-typed-results.md) | Command ledger and typed results: move command records out of the message stream into a `SessionStore` `commands` ledger (revises ADR-0050's storage/rendering mechanism, keeping its durability goal); per-command typed `CommandResult` enum (schema + `to_text()` renderer + serde, ADR-0001 `ToolOutput` precedent); `RoundEvent::CommandResult`; legacy echo messages fold into the ledger on upgrade write | Accepted |
| [0092](0092-guaranteed-activity-resolution.md) | Guaranteed activity resolution for non-round requests: the driver reconciles every control-plane dispatch back to the authoritative `HarnessState` (no more per-handler terminal-event courtesy), the TUI never paints optimistic "queued" over a running round, and `RoundEvent::Text` stops carrying a hidden lifecycle side effect (fixes the stuck `● queued` left by ADR-0088's `/autopilot` migration); §2/§3's optimistic paint for commands is retired outright by ADR-0110 | Accepted |
| [0093](0093-daemon-observability-monitor-protocol.md) | Daemon observability: a read-only monitor channel on the existing socket (`Select{Monitor}` → snapshot + diffs), a derived per-session `SessionStatus` vocabulary folded in exactly one place (`MonitorTracker`), and `neenee status [--watch] [--json] [--all]` as the first control-plane client; also fixes the unreachable `neenee daemon` parse arm | Accepted |
| [0094](0094-serve-as-host-verb.md) | Serve as the host verb: unify the session-host vocabulary (`neenee serve [--port/--public]` replaces the unreleased `daemon`; `daemon.rs`→`host.rs` converging on the existing `Host*` types; "daemon" reserved for OS deployment style) | Accepted |
| [0095](0095-standalone-session-mirroring.md) | Standalone session mirroring: a bare `neenee` TUI reports its session into the project host over a read-only `Mirror` channel (`MirrorHello` + `MonitoredSession` rows, `hosting: mirrored`), so the control plane sees every session without changing the ownership model; liveness = connection lifetime | Superseded by ADR-0096 |
| [0096](0096-unified-session-daemon.md) | Unified session daemon and control plane: one user-level daemon owns every session across all projects; UDS (default) + TCP+token (`--expose`) transports; control verbs (create/prompt/interrupt/resolve-permission/kill); `neenee` ≡ `attach` (mirror superseded); TUI `/host` panel with detach+attach switching | Accepted |
| [0097](0097-session-addressing-and-orchestrator-console.md) | Session addressing (`#N` numbers, `@n text` grammar) and the orchestrator console: dashboard gains a transcript+composer driving sessions directly or via a daemon-level orchestrator agent with an orchestration-only toolset; WIP coordination (`check_wip`/`declare_wip` so same-workspace sessions avoid conflicting verification); role-scoped `[orchestrator]`/`[summarizer]` model pins (revising ADR-0022's no-secondary-model axiom); post-round idle-session summaries with a next-direction tree | Proposed |
| [0098](0098-crate-renames-and-library-extractions.md) | Crate renames and library extractions: `neenee-core` → `neenee-contracts` (+ snake_case module normalization), `neenee-transport` → `neenee-host` (+ gains the control-plane `client` module), `neenee-tui` extracted from the CLI binary (the binary becomes a thin shell), `neenee-mcp` re-extracted from `neenee-agent` (restoring ADR-0060); dead `search_tool` and the duplicate `dirs` dependency removed | Partially superseded by ADR-0136 |
| [0099](0099-daemon-vocabulary.md) | Daemon vocabulary: bless "daemon" as the prose name for the process (revoking ADR-0094's OS-deployment reservation); the four canonical slots are process = (session) daemon, code namespace = host, verb = serve, binary = `neenee-server`; no artifact renames | Accepted |
| [0100](0100-daemon-lifecycle-standard.md) | Daemon lifecycle standard: invisible on-demand start, lifetime anchored to hosted sessions (never clients), idle-empty exit with grace, discovery-record + handshake versioning, and a `Shutdown` control verb; service-managed always-on retained as a documented deployment option | Accepted (mechanisms landed by ADR-0101) |
| [0101](0101-daemon-shutdown-correctness.md) | Daemon shutdown correctness: SIGINT/SIGTERM/SIGHUP through one injectable `ShutdownGate`, budgeted two-phase drain (advertisement pulled first, connections drained, sessions torn down concurrently with per-hook deadlines), deterministic escalation on second trigger or budget expiry, TaskBook/ConnTable ownership, RAII discovery lease + single-instance `flock`, readable bind failures, `neenee stop`, `[daemon]` config + CLI overrides, and the systemd unit as a documented option; deletes the fake `neenee-server --project` | Accepted |
| [0102](0102-unified-binary-and-runtime-rename.md) | Unified single-binary architecture and `neenee-runtime` rename: `neenee-host` → `neenee-runtime`, delete `neenee-server` crate, unify background daemon execution into `neenee serve` / `neenee serve --detach` with single-binary `current_exe()` auto-spawning | Superseded by ADR-0136 |
| [0103](0103-btw-background-asides.md) | `/btw` background asides: multi-slot side registry (asides keep running when left), Ctrl-C as the single leave-view key with Esc reserved for interrupt, a two-row page header (identity/status + view-level affordances), `/btw` + `/btw <text>` + `/btw list` grammar with an asides modal (F5), transcript back-fill on entry, and discard of never-used asides; supersedes ADR-0017's exit-and-cancel semantics | Accepted |
| [0104](0104-demand-driven-hints.md) | Demand-driven chrome hints: the head band's row-2 affordance legend renders only while a view has page-specific affordances, and no persistent surface carries the global `F1` pair (the Envoy footer legend trims to its own navigation; no main-view interrupt duplicate either) — help discovery lives on modal footers and the empty-state tour's rotating carousel (8 s pages replacing the static tagline, pinned `/connections` blocker when no provider is keyed); refines ADR-0103 §3 and consumes ADR-0057's guidance variants | Accepted |
| [0105](0105-one-port-two-protocols.md) | One port, two protocols: WebSocket control plane plus plain HTTP; bearer auth, browser Origin/subprotocol hardening, fixed port with fallback, and error codes remain, while ADR-0136 removes embedded Web assets and the panel command | Partially superseded by ADR-0136 |
| [0106](0106-command-row-interaction-and-projection.md) | Command rows: interaction by shape — width-aware `Plain`/`Inline`/`Disclose` layouts replace the one-size disclosure block (no `⚙`, `+` only when a body exists, inline ` · ` reply join), and the live transcript is defined as a projection the ledger renders into (placement in sequence, per-row view state, model-invisible); revises ADR-0091 D4 | Accepted |
| [0107](0107-trust-gate-covers-project-skills-and-commands.md) | Trust gate covers project skills and slash commands: the ADR-0085 §5 trust decision now gates Repo-scope skills (`.neenee/skills` et al.) and `.neenee/commands/` at scan time (project-root anchored, daemon-correct); `/trust` enables mid-session, `/untrust` drops immediately; same-name overrides emit a one-time shadow warning notice | Accepted |
| [0108](0108-one-command-component-input-output-lifecycle.md) | One command component: input and output in one row, with a lifecycle — a slash command is never echoed as a user bubble; dispatch pushes one optimistic `⌘` row that settles in place with its reply (`Pending`/`Completed`/`Cancelled` projection phases), and resume folds durable echoes into the ledger rows; revises ADR-0106 §2 and ADR-0091 D4's live surface | Accepted |
| [0109](0109-command-card-and-triangle-disclosure.md) | Command rows are cards; the disclosure glyph is `▸`/`▾`: a command component paints a full-width band (`Theme::command_surface`, hover-lifted) with a thick `┃` identity bar in the family tone and a fixed reserved marker slot, so an operation is separated from prose by shape before color — and every disclosure site (tools, reasoning, retries, commands, sticky pins) migrates `+`/`-` → `▸`/`▾`, reserving `+`/`-` exclusively for diff signs and counts; revises ADR-0108 §1 and ADR-0106 §1's marker | Accepted |
| [0110](0110-commands-do-not-trigger-the-activity-bar.md) | Commands do not trigger the activity bar: a slash command is outside the round state machine, so `SendSlash` performs no activity-bar arming (no `is_responding`, no transient `"queued"`, no fabricated interrupt affordance) and command handlers stop borrowing `RoundEvent::Activity` (`/compact`; round-owned emitters keep it); the ADR-0108 pending row is a command's in-flight feedback, and the driver reconcile stays as a frontend-agnostic safety net; revises ADR-0092 §2/§3 | Accepted |
| [0111](0111-transcript-entry-unification-and-concurrent-rendering.md) | Transcript entry unification and concurrent rendering: formalize the transcript as an ordered stream of `Entry`s (Turn Entry, Command Entry) with equal granularity; Header as boundary/separator (`⌘ /cmd · HH:MM` or `> turn N / prompt · HH:MM`) followed directly by flat, unfolded body content (collapsible folding overturned); multi-entry concurrent rendering allows command dispatch while turns stream, with dynamic vertical height staging naturally pushing downstream entries downward | Accepted |
| [0112](0112-client-declared-session-end.md) | Client-declared session end: a new `AgentRequest::EndSession` wire message (intercepted at the connection layer, never queued to the driver) lets a client that is *done* — not detaching — end its hosted session deterministically; the TUI's `/exit` and double-`Ctrl+C`, headless runs' terminal round, and the web panel's new end-session action all funnel into the existing `kill_session` teardown (driver cancelled, `SessionEnd` hooks, WIP cleared, `SessionRemoved` published so dashboards drop the row); disk history kept; plain socket drops keep detach semantics (ADR-0096) | Accepted |
| [0113](0113-task-supervision-and-idle-suspension.md) | Task supervision and idle hosted-session suspension: per-task panic policies replace fire-and-forget spawns — driver panics evict the hosted session through the standard teardown after broadcasting a visible `Error` (no more zombie entries with un-drained unbounded `req_tx`), the monitor tap isolates per event, round tasks defer their lifecycle cleanup through a caught panic, the `/schedule` tick restarts with bounded backoff, and a daemon-wide panic hook logs every panic with origin; idle hosted sessions (no receivers, inactive, 30min quiet) are *suspended* — torn down in memory without `Exit` or SessionEnd, resuming via lazy-resume — bounding daemon memory by active work; process-group kills close the grandchild leak (bash/hook/MCP); the TUI gains a main-thread panic hook and an async `@` scan; the doom guard defaults on with normalized signatures | Accepted |
| [0114](0114-identity-addressed-stream-appends-and-flex-layout-foundation.md) | Identity-addressed stream appends and the flex layout foundation: every streaming append/finalize (reasoning deltas, text deltas, `StreamEnd`/`StreamReasoningEnd`, envoy child folding) resolves its target entry by scanning backward for `(round, turn)` + kind instead of `msgs.last_mut()` position, so a command row or notice appended mid-stream can no longer fork a Thinking trace into two entries (delivering ADR-0111's concurrent-rendering contract); the engine gains a pure-Rust flexbox-style layout subsystem (`neenee_tui_engine::flex`: direction/justify/align/gap/grow/shrink/basis/min-max + a `measure` intrinsic-size callback, integer-exact with `usize` offsets) with `Layout::split` re-implemented as a thin mapping onto it, and `build_virtual_index` derives chunk line offsets from the same solver so virtualization and painting share one geometry source | Accepted |
| [0115](0115-credential-placement-config-vs-state.md) | Credential placement under XDG: token auth is config, OAuth tokens are state — `credentials.toml` (user-supplied API keys, portable, manually editable) stays in `$XDG_CONFIG_HOME` while `auth.toml` (daemon-rewritten OAuth token sets) stays in `$XDG_STATE_HOME`, classified by provenance and churn per the spec's own importance/portability test, with security (0600, private temp writes) orthogonal to the split | Accepted |
| [0116-2](0116-2-session-lineage.md) | Session lineage: forks and asides are first-class — a persisted `SessionForkKind` (`Trunk`/`Fork`/`Aside`) stamped at fork time flows through `SessionSummary` → `SessionOverview` → `MonitoredSession`, the dashboard badges branches (`⑂aside`/`⑂fork`, muted name) while the trunk stays the single unbadged main line, and `/btw` views gain view-scoped chrome (`SessionChrome` per session: own activity bar/round/elapsed, primary's state parked and restored on exit) | Accepted |
| [0117](0117-native-cl100k-bpe-tokenizer.md) | Native cl100k_base BPE tokenizer for token prediction: the char-class estimator behind context pressure, compaction gates, and `/context` is replaced by a dependency-free, tiktoken-faithful BPE (hand-rolled cl100k pretokenizer scanner + `byte_pair_merge` over a compactly packed 100 256-rank vocabulary embedded from `vendor/cl100k_base.packed`, 1.04 MB), with chat framing overhead per message/tool-call — measured −24…−54% estimator error on this repo's own CJK+code corpus drops to exact; the estimator remains as a documented cheap fallback | Accepted |
| [0118](0118-two-stage-web-research-search-breadth-fetch-depth.md) | Two-stage web research: `websearch` stays breadth-only (pluggable `SearchProvider`), `webfetch` becomes the pluggable depth stage via a new `Reader` abstraction (`[websearch] reader = "builtin" \| "jina"`, with builtin fallback + failure annotation) — multi-provider breadth/depth is two orthogonal config axes, not a fused pipeline; SSRF runs before any reader, truncation unified in bytes at 16k; Tavily breadth upgraded to `search_depth: "advanced"` | Accepted |
| [0119](0119-cli-verb-vocabulary-and-lifecycle-budgets.md) | CLI verb vocabulary and lifecycle budget coordination: one noun per resource (`daemon start [--fg]/stop/status`, `session ls/rm`, `attach [id]`, `run`) with retired spellings refused by errors that teach; the parser moves from `neenee-runtime` to a declarative spec table in `neenee-cli::cli` (parse + help + "did you mean" + completions from one source, closing the `serve`/`daemon start` flag-drift class); `attach` with no id opens the real TUI sessions picker over a throwaway `AttachAction::Picker` carrier; the daemon publishes its drain budget as `Discovery.grace_secs` so `daemon stop` waits the daemon's own grace before SIGTERM/SIGKILL (an early signal forces the daemon out of graceful drain), Tier-4's UDS unlink gains the pid guard the record already had, and the dead `--single-instance` flag is removed | Accepted |
| [0120](0120-tokens-first-class-unit.md) | Tokens are the first-class unit: the prune/compact pipeline goes token-native (`prune_tool_results` takes token budgets, `reclaimed_tokens`, token tier thresholds), model-visible markers (`(42 lines, 350 tokens)`, `[... N tokens elided ...]`) and tool-output truncation notices report tokens, transcript displays (`Thinking · N tokens`, `Compacted: N → M tokens`) drop the chars/bytes lie, `Compacted{before_tokens,after_tokens}` replaces the byte-valued `*_chars` fields (serde aliases replay old records), and the char-class estimator + `CHARS_PER_TOKEN` are deleted outright | Accepted |
| [0121](0121-instance-isolation-for-development-and-testing.md) | Instance isolation for development and testing: one selector with two entrances — the `--home <dir>` flag and the `NEENEE_HOME` env var — moves the entire footprint (config/data/state/cache under `<dir>/neenee/*`, daemon runtime files under `<dir>/neenee/instance` via the new `Dirs::instance_dir()`, the single daemon-facing derivation), `NEENEE_PORT` overrides the well-known 9800 default, and auto-spawned daemons inherit the sandbox by construction | Accepted |
| [0122](0122-durable-cross-session-usage-statistics.md) | Durable cross-session usage statistics: a day-partitioned append-only store at `data/usage/daily/<day>.json` (a sibling of `projects/`, so session cleanup can never touch it) mirrors every terminal request via a `UsageStatSink` hooked into `TokenSourceLedger::settle_request`; `/usage` opens the aggregated report (per-day totals, per-model breakdown, recent event log) | Accepted |
| [0123](0123-provider-instances-as-state-derived-routes.md) | Provider instances are state; routes are derived, never persisted: `config.toml` becomes behavior-only (selection + prefs), instances move to a state store (`providers.toml`), credentials are keyed by instance (`credentials.toml [providers.<id>]`), and per-model routes are derived at runtime from each instance's template + the discovery cache (per-(instance, model) reasoning in `route_settings`) — so two instances of one template never duplicate or drift a route set, and discovery stops mutating configuration | Accepted |
| [0124](0124-web-tool-hardening-per-hop-ssrf-token-budgets-untrusted-boundary.md) | Web tool hardening: per-hop SSRF (redirects followed manually via `guarded_get`, every hop re-guarded, streamed bodies with an 8 MiB cap, missing ranges `fe80::/10`/TEST-NET/`240.0.0.0/4` closed), token budgets everywhere (`SearchProvider` returns structured `ProviderOutput` so the tool layer degrades snippets before ever dropping title+URL entries; `webfetch` joins the 4 000-token cap and its truncation notice stops suggesting `raw=true`), and an untrusted-content boundary (`[BEGIN/END UNTRUSTED WEB CONTENT]` markers + `system.web_untrusted_content` prompt section; Tavily key to `Authorization`; unknown provider names warn) | Accepted |
| [0125](0125-daemon-terminal-survival-and-armed-schedule-rehost.md) | Daemon terminal survival and autonomous-session rehost: detached daemons call `setsid(2)` (a new session, not just a new process group) so terminal/compositor death cannot SIGHUP them — tmux-server semantics; the `/schedule` scheduler takes the session's teardown token, dispatch is deliver-first/mutate-second (an undeliverable fire stays armed instead of consuming a cron interval or dropping a once-job), idle-suspension exempts sessions with armed jobs, and the daemon rehosts every persisted session that still has armed `/schedule` jobs at boot (`sessions_with_armed_schedules` header scan + `rehost_armed_sessions`, gated by `[daemon] rehost_armed_schedules = true`) so scheduled prompts keep firing across daemon restarts | Accepted (detachment superseded by ADR-0129) |
| [0126](0126-queue-affordances-ctrl-row-transcript-inserts-queue-pointer.md) | Queue affordances: the queue family moves to the Ctrl row (`Ctrl+Q` expand, `Ctrl+P` block/resume, `Ctrl+O` insert) because Fn-dispatch is terminal policy the app cannot rely on; a mid-round insert becomes a **transcript-owned** entry at send time (`DeliveryStatus::Queued`, `insert_id` correlation, settled in place by `UserInputInserted`/`HeldNextRound`/`NextRoundStarted`) so the `Inserting` outbox state and its shadow item are deleted — inserts accept no queue operations; and `↑`/`↓` walk a **non-destructive queue pointer** over the outbox whose Enter commits an edit **in place** (a vanished target falls through to an ordinary send) | Accepted |
| [0127](0127-durable-round-interrupt-records.md) | Durable round-interrupt records: every stop path (user Esc Esc, superseded by newer input/session switch, terminated with the process) parks its reason on `RoundLifecycle` for the unwinding round's tail to read back, persists one `RoundInterrupt { reason, at_ms, round }` as session-store **projection state** (schema v11; never model-visible, zero context tokens — the no-context-marker decision stands), emits `RoundEvent::RoundInterrupted`, and re-projects the records into the transcript on resume at their timestamp seams (`▲ interrupted · HH:MM` / `round N · reason`); kill paths record before dropping the driver and hard crashes are inferred from abandoned in-flight requests, so the previously invisible supersede and the unexplained post-crash dangling round both become answerable at a glance | Accepted |
| [0129](0129-single-primitive-unix-daemon-detachment.md) | Single-primitive Unix daemon detachment: both daemon spawn paths share one helper that calls `setsid(2)` alone; never combine it with `process_group(0)`/`setpgid(2)`, which makes the child a process-group leader and forces `setsid(2)` to fail with `EPERM` | Accepted |
| [0130](0130-native-platform-capability-boundary.md) | Native platform capability boundary: platform-neutral business policy over explicit Unix and Windows adapters for paths, IPC, locking, process lifetime, shells, and private files | Accepted |
| [0131](0131-elastic-upstream-model-passthrough-and-thinking-disclosure.md) | Elastic upstream: model ids pass through verbatim; chain-disclosure refusal downgrades once per channel | Accepted |
| [0132](0132-session-persisted-autopilot-posture.md) | Session-persisted autopilot posture: the unattended/attended flag moves from a process-local `Mutex<bool>` to session-scoped persisted state (`SessionEvent::AutopilotSet`, snapshot semantics, ADR-0048 Phase 2 pattern) so a daemon crash/kill/upgrade reopens the session in the posture it died in; every write path persists (`/autopilot`, `--autopilot`, `/principal`), every restore path restores (bootstrap resume, attach snapshot, in-process switch), `/reset` de-escalates, a toggle alone never materialises an empty session, and pre-ADR-0132 sessions are recovered from the command ledger with a loud notice | Accepted |
| [0133](0133-view-surfaces-buffer-like-lifecycle-and-quick-switch.md) | Views as retained, buffer-like surfaces: every browse modal becomes a retained `View` whose scroll/index/sub-layer state survives hide (first-open initialisation replaces the per-open reset ritual), navigation becomes one bounded MRU stack (replacing the hand-mirrored deepest-first Esc chain, `editor_return_to`, and the hard-coded return links) with a shared hide/close/switch vocabulary, composer-line views park drafts per-view instead of one global `stashed_input`, and a Ctrl+L quick switcher lists all views fuzzy-MRU | Superseded by ADR-0139 |
| [0134](0134-wire-protocol-negotiation.md) | Wire-protocol window negotiation over product-version equality: the wire envelope moves to `neenee-contracts` with its own integer `PROTOCOL_VERSION`/`MIN_PROTOCOL_VERSION`; a client declaring `Select{protocol}` is served anywhere in the window regardless of product build (refused outside it with `code:"protocol_mismatch"`), protocol-less clients keep ADR-0100 rule 4's exact equality, the discovery record carries `protocol` for pre-handshake refusal, additive changes ship without a bump (CI's `check-wire-compat.sh` enforces bump-or-`wire-compatible`-label), and the web panel's `AttachAction`/`ControlRequest`/`SessionOverview` mirrors become ts-rs-generated | Accepted |
| [0135](0135-retirement-deletes-no-teaching-errors.md) | Retirement deletes: a retired CLI spelling has no parse-tree presence — no teaching error, no alias, no probe — so `serve`/`stop`/`status`/`resume`/`exec`/`session ls` fall through to the generic unrecognized-command error (or parse as positional prompts) exactly like any typo, superseding ADR-0119's "retirement teaches" posture whose strings drifted into naming commands the same binary refuses; error messages may only name canonical spellings the binary accepts | Accepted |
| [0136](0136-muta-core-and-mutx-terminal-app.md) | Muta core and peer frontend apps: hard rename, `muta` daemon/service core, `mutx` under the TUI subproject with on-demand core startup, backend-owned completion, and independently deployed Web assets | Accepted |
| [0137](0137-server-side-kv-cache-alignment-and-zoning.md) | Server-side KV cache alignment and zoning: three-zone request architecture (Zone 1 static prefix with sorted tool specs and invariant system prompt, Zone 2 monotonic history stream with lossless old-turn tool compaction, Zone 3 ephemeral tail for dynamic metadata), native in-process regex search, persistent PTY terminal sessions, and Inflight concurrency deduplication | Accepted |
| [0138](0138-actor-model-subagent-isolation-and-mcp-sandboxing.md) | Actor-model subagent isolation and MCP sandboxing: lean principal agent with permanently invariant core toolset (100% KV cache stability in 200k+ sessions), dynamic/heavy MCP tool sandboxing inside ephemeral Envoy subagents, zero-pollution noise/error boundary, and parallel actor execution | Accepted |
| [0139](0139-unified-tui-surface-router-and-view-lifecycle.md) | Unified TUI surface router and complete view lifecycle: exact `ViewId` identity, one authoritative foreground/return stack, create/show/hide/switch/close semantics, refresh-on-show effects, stacked request sheets, explicit switcher close, and data responses separated from navigation signals | Accepted |
